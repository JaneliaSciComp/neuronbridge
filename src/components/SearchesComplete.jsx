import React from "react";

// TODO: Through polling or websockets, check the bucket for searches
// that have completed grab the meta data and display the searches
// on the page, with a link to see the full results.

export default function SearchesComplete() {
  return <p>List of searches that are complete and ready to view</p>;
}
